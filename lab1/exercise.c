// Write a program to take name as input from user ?

#include <stdio.h>
int main()
{
char name[50];
printf("Enter your good name (without any space) :: ");
scanf("%s", name);
printf("%s", name);
return 0;
}
